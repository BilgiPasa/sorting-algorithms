Use this command in this directory to compile the Java files:
    javac Main.java --release 25

Use this command to run the Java files that you compiled:
    java Main.java

If you are going to compile or run the Java files, make sure that you have Java installed.
(Java 25 or a more recent Java version is recommended.)
