Use this command in this directory to compile the Java code:
    javac Main.java --release 25

Use this command to run a Java file:
    java Main.java

If you are going to compile or run the Java code, make sure that you have Java 25 installed.
